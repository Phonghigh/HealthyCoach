import { Module } from '@nestjs/common';

/**
 * Training plan CRUD + generation orchestration. Empty scaffold —
 * implemented in later phases, will import from src/rules.
 */
@Module({})
export class PlansModule {}
