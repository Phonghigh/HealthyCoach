# Local dev defaults — matches docker-compose.yml postgres service at repo root.
DATABASE_URL="postgresql://healthycoach:healthycoach@localhost:5432/healthycoach?schema=public"

# Random 32+ byte shared secret. Generate with: openssl rand -hex 32
DEVICE_KEY="replace-with-a-random-32-plus-byte-secret-value"

PORT=3000
NODE_ENV=development
